"""
def some function(some queries):
	if (no queries):
		return (error: no queries found)
	if (database is empty):
		return (error: database is empty)
	
	process queries to obtain results

	if (distances are similar):
		find top 3 (?) best matches
		return (results as a list)
	else if (distances are too different):
		return (no matches found)
"""

